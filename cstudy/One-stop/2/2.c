#include <stdio.h>

int main(void)
{
    char firstletter; //定义一个字符型变量
    int hour,minute; // 定义两个整数变量
    firstletter = 'a'; //给变量赋值
    hour = 11;
    minute = 59;
    /* code */
    printf("Current time is %d:%d\n",hour,minute);
    return 0;
}
